# LEAPP Media Format Samples

Simple labeled media files for testing LEAPP `check_in_media()` and LAVA preview behavior.

Suggested artifact columns:

- Type
- Image
- Notes

Each sample image has visible text showing its type, MIME value, and extension.

The JPEG XL sample is `sample_jxl.jxl` with expected MIME `image/jxl`.
